# server port config
server:
  port: 8080  # server port

file:
  upload-dir: C:\uploads\profile-image
spring:
  mail:
    host:
    port:
    username:
    password:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  # db config
  jwt:
    secret: vmfhaltmskdlstkfkdfdfkgfjsjdfgjsghewkwenbfehfdjhckdkdfjfsfdhjsfduh
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
#    url: jdbc:mysql://host.docker.internal:3306/todo               # db url
    url: jdbc:mariadb://180.67.117.226:3306/todo               # db url
    username: feeleat                    # db username
    password: root                     # db password
  devtools:
    restart:
      enabled: false # auto restart option enabled


  # jpa config
  jpa:
    hibernate:
      ddl-auto: none      # DDL ???? ????
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org:
      springframework:
        security: DEBUG
